# [Asset type] Workflow

<!-- Copy this file for every new workflow doc. Delete the comments as you fill it in.
     The done test: Diego, Gero, or Francis could produce this asset, to oaki's standard,
     using only this doc. If a step needs Demi in the room, the doc is not finished. -->

**Status:** draft | proven (pilot) | stable
**Last updated:** YYYY-MM-DD
**Owner:** (who maintains this doc)

## Purpose & when to use

<!-- One paragraph: what this workflow produces and the trigger for running it.
     Example: "Produces the portrait book cover for a case study. Run once per project,
     after the grading standard has been applied to the hero render." -->

## Inputs required

<!-- Be specific about the source of each input:
     - From Diego / the archives: which files, what minimum resolution, which project folder
     - From the client: approvals, footage, confidentiality state
     - From another workflow: e.g. "graded hero still (F2 output)"
     - Decisions that must already be made, with a pointer to where they live -->

## Tools & models

<!-- Name them exactly. Which Higgsfield model and preset, gpt-image-2 and whether Thinking Mode,
     Firefly / Nano Banana, 3ds Max + Corona, Magnific settings, Topaz model, Photoshop steps.
     Version and plan tier if it affects output. -->

## Procedure

<!-- Numbered, reproducible steps. Every step has a verb and a checkable output.
     If a step branches ("if the render is under 3000px wide..."), write the branch.
     No step may read "art-direct until it looks right" without defining what right means
     (point to the Quality bar). -->

1.

## Prompt templates / parameters

<!-- The actual scaffolds, with variables marked: {project}, {ground_color}, {city}, {phase}.
     Include the settings that matter (creativity/resemblance values, camera preset names,
     denoise levels), not just the prompt text. -->

## Quality bar

<!-- Tie to the 6-phase framework (Vision · Spirit · Human Trace · Detail · Experience · Synthesis)
     and the oaki frame (F2): editorial, lush, color-confident, refined materials, no warped
     geometry, no invented context. For anything with words: passes oaki-copywriting
     (English, no em dashes, banned-word list, no brand yardsticks, never justify pricing).
     Write the REJECT conditions, not just the accept ones. -->

## Output spec

<!-- Dimensions, format, compression target, color space, file naming (per F3),
     where the file lives (repo path / Sanity field / Drive folder), who loads it. -->

## Time & cost estimate

<!-- Per asset, once the workflow is warm: person-minutes by role, credits/tokens consumed,
     wall-clock including render/generation waits. Update after the pilot with real numbers. -->

## Reference example

<!-- Link the first asset produced with this doc, once it exists. A workflow without a
     shipped reference is a theory. -->

## Known failure modes / gotchas

<!-- What went wrong during the pilot and how to detect it early. Model-specific quirks,
     settings that look fine at thumbnail size and fail at full-bleed, steps people skip. -->

## Open questions

<!-- Anything unresolved, each with who decides it. -->
